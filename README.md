# Fower_classify
A lightweight web application that predicts the species of an Iris flower based on  sepals and petals.This project demonstrates an end-to-end Machine Learning pipeline—from a trained K-Nearest Neighbors (KNN) model to a functional REST API and a user-friendly web interface.
